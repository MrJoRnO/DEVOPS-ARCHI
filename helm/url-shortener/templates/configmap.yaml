apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "url-shortener.fullname" . }}-config
  labels:
    {{- include "url-shortener.labels" . | nindent 4 }}
data:
  APP_ENV: {{ .Values.envName | default "development" | quote }}
  LOG_LEVEL: "info"