apiVersion: v1
kind: Secret
metadata:
  name: {{ include "url-shortener.fullname" . }}-secret
  labels:
    {{- include "url-shortener.labels" . | nindent 4 }}
type: Opaque
data:
  # Base64 encoded 'password' - for demonstration only
  DB_PASSWORD: {{ "cGFzc3dvcmQ=" | quote }}